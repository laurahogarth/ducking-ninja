-if traveller_signed_in?
  =link_to("Log Out", logout_path, :method => :delete, :class => :button)
-elsif agent_signed_in?
  =link_to("Log Out", agent_logout_path, :method => :delete, :class => :button)
-else
  =link_to("Get Started!", login_path, :class => :button)

